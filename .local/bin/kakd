#!/usr/local/bin/fish 

set server (basename (pwd) | tr -d '.')
if kak -l | grep $server >/dev/null
  kak -c $server $argv
else
  tmux new -d -s $server "kak -d -s $server" && kak -c $server $argv
end
